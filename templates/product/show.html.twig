{% extends 'base.html.twig' %}

{% block title %}Product Details{% endblock %}

{% block body %}
  <div class="container mx-auto my-8 p-4">
    <h1 class="text-4xl font-bold text-center mb-6">Product Details</h1>
    
    <div class="bg-white shadow-md rounded-lg p-6 border border-gray-300">
      <h2 class="text-2xl font-semibold text-gray-800 text-center mb-4">{{ product.name }}</h2>
      <p class="text-gray-600 text-base mb-4">{{ product.description | raw }}</p>
      <p class="text-lg font-bold text-gray-900 text-center">{{ product.price | number_format(2, '.', ',') }} €</p>
    </div>
  </div>
{% endblock %}
